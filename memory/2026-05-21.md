# 2026-05-21 작업 일지

## 세션 개요
이전 컴팩션에서 이어받아 진행. Phase 2 미완료 이슈 + 새 UX 요청 3건 처리.

---

## 완료된 작업

### Phase 2 버그 패치 (이전 세션 완료 - 본 세션 인수)
1. **TrackBlock 스크롤 버그** - `e.preventDefault()` 추가
2. **Repaint 결과 isActive=false 로직 제거**
3. **Length 슬라이더 step=10s 적용**
4. **180s 레이블 CSS 수정**
5. **Soft Cancel** - `useJobStore.cancelJob()` 구현
6. **생성 중 Undo/Redo + Ctrl+Z/Y 차단**

### 본 세션 새 작업 1: Timeline Seek / Scrubbing (완료)
- `usePlaybackStore.ts`에 `setCurrentTime` 액션 추가
- `TimelineRuler.tsx`에 마우스 드래그 이벤트 바인딩
  - mousedown → pause(), 드래그 중 플레이헤드 실시간 이동
  - mouseup → seekTo() + play() 재개
  - cursor: ew-resize, user-select: none 처리

### 본 세션 새 작업 2: 드래그 락 / 무한 툴팁 버그 해결 (완료)
- `SelectionOverlay.tsx` 에러 툴팁에 ✕ 닫기 버튼 추가 (stopPropagation 포함)
- `TrackRow.tsx`에 Drag Threshold 5px 적용 → 단순 클릭 시 툴팁 미발동
- `TrackRow.tsx` mousedown 첫머리에 `clearSelection()` 호출 → 빈 곳 클릭 시 툴팁 즉시 소멸

### 본 세션 새 작업 3: 음악 생성 진행률 정교화 (완료)
**백엔드 `acestep_client.py`**
- 폴링 간격 3s → 0.5s로 단축
- `/query_result` 응답 내 `result` JSON의 `progress` 필드 읽어 실제 진행률 전달
- 실제값 있으면 heuristic 추정치 미전송 (`real_progress_notified` 플래그)

**프론트엔드 `glassmorphism.css`**
- `transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1)` (기존 0.3s ease)
- shimmer 애니메이션 추가: 보라-파랑 그라디언트 흐름

**프론트엔드 `JobMessageView.tsx`**
- `stageName()` 함수 추가: progress % 기준 단계별 한글 텍스트
  - 0-10%: "큐 대기 중...", 10-30%: "AI 모델 초기화...", 30-70%: "생성 중 (X%)", 70-95%: "마무리 중 (X%)", 95%+: "거의 완료..."
- 배지/버튼 텍스트 한글화 (완료/실패/생성중/대기중/닫기)

---

## 미완료 / 잔여 이슈

### 이슈 #3: 100% 줌 가로 overflow (미해결)
- 증상: 브라우저 100% 줌에서 가로 잘림 발생
- 원인: Phase 1.4 픽셀 레이아웃 도입 후 발생
- 방향: 외부 컨테이너에 `max-width: 100vw + overflow-x: auto`
- **→ 다음 세션에서 처리 필요**

### ACE-Step progress 필드 실제 지원 여부 미확인
- `query_result_service.py` 분석 결과: `result` 내부 JSON에 `progress` 필드가 있을 수도 없을 수도 있음
- running 상태(status=0)일 때 result가 비어있을 가능성 있음
- 실제 생성 시 로그에서 "Real task progress: X%" 메시지 뜨면 연동 성공
- 없으면 heuristic fallback으로 동작 (기능 저하 없음)

---

## 서버 상태 (세션 종료 시점)
- 8002 (백엔드 FastAPI): LISTENING ✅ (task-1996)
- 5173 (Vite 프론트엔드): RUNNING ✅ (task-1629)
- 8001 (ACE-Step 1.7B): RUNNING ✅ (task-1730)

---

## 다음 세션 우선순위
1. 이슈 #3 가로 overflow 수정
2. ACE-Step progress 실제 연동 검증 (음악 생성 한 번 돌려보고 로그 확인)
3. soo_todo.md 의 사용자 결정 대기 항목 처리